name: Laravel

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  laravel-tests:

    runs-on: ubuntu-latest

    steps:
    - uses: shivammathur/setup-php@15c43e89cdef867065b0213be354c2841860869e
      with:
        php-version: '8.3'
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
    - uses: actions/checkout@v4
    - name: Copy .env
      run: php -r "file_exists('.env') || copy('.env.example', '.env');"
    - name: Install Dependencies
      run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist
    - name: Generate key
      run: php artisan key:generate
    - name: Directory Permissions
      run: chmod -R 777 storage bootstrap/cache
    - name: Create Database
      run: |
        mkdir -p database
        touch database/database.sqlite
    - name: Fill database
      env:
        DB_CONNECTION: sqlite
        DB_DATABASE: database/database.sqlite
      run: php artisan migrate --seed
    - name: Npm build
      run: npm install && npm run build
    - name: Execute tests (Unit and Feature tests) via PHPUnit/Pest
      env:
        DB_CONNECTION: sqlite
        DB_DATABASE: database/database.sqlite
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      run: php artisan test

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: laravel-tests
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main' && success()
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4

  #   # Set up SSH key
  #   - name: Set up SSH key
  #     uses: webfactory/ssh-agent@v0.5.3
  #     with:
  #       ssh-private-key: ${{ secrets.ALISA_SSH_PRIVATE_KEY }}

  #   # Deploy to Production server
  #   - name: Deploy to Production Server
  #     run: |
  #       ssh -o StrictHostKeyChecking=no alisa@alisa.fit << 'EOF'
  #         cd /home/alisa/web/alisa.fit/public_html
  #         git pull origin main
  #         /usr/bin/php8.3 /usr/local/bin/composer install --no-dev --optimize-autoloader --no-interaction
  #         npm install && npm run build
  #         /usr/bin/php8.3 artisan migrate --force
  #         /usr/bin/php8.3 artisan config:cache
  #         /usr/bin/php8.3 artisan route:cache
  #         /usr/bin/php8.3 artisan optimize
  #         /usr/bin/php8.3 artisan icon:cache
  #         npm run build
  #       EOF
  #     env:
  #       SSH_PRIVATE_KEY: ${{ secrets.ALISA_SSH_PRIVATE_KEY }}